{
  "name": "intercompany-netting",
  "version": "0.1.0",
  "private": true,
  "description": "Multilateral intercompany netting with atomic Canton settlement and TypeSafe-assisted ingest.",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "demo:seed": "pnpm --filter @netting/core demo:seed"
  },
  "devDependencies": {
    "@types/node": "22.7.4",
    "typescript": "5.6.3"
  }
}
